[api]
data_path = "{{pkg.svc_data_path}}"
log_path = "{{pkg.svc_var_path}}"
key_path = "{{pkg.svc_files_path}}"
{{toToml cfg.api}}

[github]
app_private_key = "{{pkg.svc_files_path}}/builder-github-app.pem"
{{toToml cfg.github}}

[http]
{{toToml cfg.http}}

[oauth]
{{toToml cfg.oauth}}

{{~#each bind.router.members as |member|}}
[[routers]]
host = "{{member.sys.ip}}"
port = {{member.cfg.port}}
{{~/each}}

[s3]
{{toToml cfg.s3}}

[segment]
{{toToml cfg.segment}}

[ui]
root = "{{pkg.svc_static_path}}"

[upstream]
{{toToml cfg.upstream}}

{{~#each bind.memcached.members as |member|}}
[[memcached]]
host = "{{member.sys.ip}}"
port = {{member.cfg.port}}
{{~/each}}

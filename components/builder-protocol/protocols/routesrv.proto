syntax = "proto2";
import "net.proto";
package routesrv;

message Disconnect {}
message Heartbeat {}

message Registration {
  reserved 2;
  reserved "shards";
  optional net.Protocol protocol = 1;
}
